name: "CodeQL - Project 1"
on:
  push:
    branches: [ "main" ]
    paths:
      - project1/**
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main" ]
#   schedule:
#     - cron: '43 3 * * 5'

jobs:
  call-workflow-passing-data:
    uses: cleydson/monorepo-filtering-workaround/.github/workflows/codeql.yml@main
    with:
      WORKING_DIRECTORY: .github/labeler.yml
      SCRIPTING_LANGUAGES: 'java'
